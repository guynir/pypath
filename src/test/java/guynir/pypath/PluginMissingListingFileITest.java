package guynir.pypath;

import com.intellij.testFramework.fixtures.BasePlatformTestCase;

/**
 * Integration test for use-case when the plugin starts without "source_dirs" file.
 *
 * @author Guy Raz Nir
 * @since 2020/02/08
 */
public class PluginMissingListingFileITest extends BasePlatformTestCase {

    public void testMissingListingFile() {
        // Test body is empty. The test itself makes sure the plugin does not crash when no listing file exists.
    }
}
